<div id="hidden">
    <h5>Some times it is useful to record whether or not a user has invoked a given JavaScript trigger. For example, we may not want to let the user press button B until they have pressed button A. Both button A and B invoke JavaScript triggers which call the function pressButton; however each passes its letter as a param. :</h5>
     <pre>
        var hasPressedA = false; 
        function pressButton(button){
            if (button == "A"){
                hasPressedA = true;
                api.wa_executeConsoleCommand("displayHUDMessage you pressed A",3000);
            } else if (button == "B" AND hasPressedA){
                api.wa_executeConsoleCommand("displayHUDMessage you pressed B",3000);
            } else {
                api.wa_executeConsoleCommand("displayHUDMessage you pressed an invalid button",3000);
            }
            
        }
     </pre>
     <img id="hiddenElement" class="left hidden" src="/assets/waldo.png">
     <div class="btn info next" id="next">
          Next >>
     </div>
</div>
<script>
    $("#hidden").css("margin-top",$(".tut-container").height()/2 - $("#hidden").height()/2);
    $("#next").click(function(){
        $.get('/params', function(data) {
            $("#hidden").fadeOut();  
            setTimeout(function(){$("#tut-container-content").html(data);},1000);
        });
    });
</script>
