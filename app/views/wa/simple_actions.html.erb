<div id="call">
    <h2>Now that we have our api object, we can start calling functionality inside Web Alive. To do this, we will call the</h2>
    <pre>wa_executeConsoleCommand(command, duration)</pre> 
    <h2>method on our api object. The method takes in two parameters, the command to execute as a string and an integer representing the duration in milliseconds to increase the frame rate of Web Alive.</h2>
    
    <h2> Now let's try some commands! </h2>
    <div class="btn info next" id="gesture">
        Gestures >>
    </div>
</div>


<script type="text/javascript">
    $(document).ready(function(){
        $("#call").css("margin-top",$(".tut-container").height()/2 - $("#call").height()/2);
        $("#gesture").click(function(){
            $.get('/gesture', function(data) {
                $("#call").fadeOut();
                
                setTimeout(function(){$(".tut-container").html(data);},1000);
            }); 
        });
    });
    
</script>